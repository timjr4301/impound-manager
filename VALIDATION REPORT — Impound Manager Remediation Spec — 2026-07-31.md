# VALIDATION REPORT

**Validating:** BUILD — Impound Manager Remediation — Spec — 2026-07-31 (+ HANDOFF — Build Manager)
**Validated against:** timjr4301/impound-manager @ `a060865` (main, verified latest commit; zero open PRs — all 21 PRs closed, #15 closed unmerged)
**Validator:** independent Cowork session (Tim's instance), 2026-07-31. Repo read via GitHub connector: task_engine.py, letter_engine.py, letter_triggers.py, models.py (full), towbook_import.py, blueprints/audit.py, permissions.py, render.yaml, reset_users.py, MASTER_CONTEXT.md (full), Report-impounds (5).csv (headers), PR list #1–#21, commit d5f8e3a. Not read line-by-line: app.py (158KB) and blueprints/heather.py (76KB) — claims touching them verified via targeted code search and PR records; anything that couldn't be pinned is marked NOT FOUND below.

---

## 1. VERDICT

**SOUND WITH AMENDMENTS** — the plan's structure, sequence, and safety rails hold, but spec §3's "three conflicting definitions" is partially misread (two of the three legs actually agree), and WP-0's STALE-marking instruction, built on that misread, would strike compliance text that matches the live code — fix the doc before Launch Card #1 goes out.

---

## 2. FINDINGS TABLE

| ID | Check | Severity | The claim as written | What the code actually shows | Proposed fix |
|---|---|---|---|---|---|
| V-1 | 2 | **BLOCKER** | §3: "Three conflicting definitions of the 60-day title clock… MASTER_CONTEXT.md (60 days from `impound_date`, plus 30 after Letter 2), and `models.py` (`title_eligible_date` computed off `letter2.sent_date` for PPI)" | These two legs **agree**. models.py:805–812: PPI `title_eligible_date = max(impound_date + 60, l2.sent_date + 30)` (`PPI_TITLE_FROM_IMPOUND = 60`, `PPI_TITLE_FROM_LETTER2 = 30`, models.py:14–15) — exactly MASTER_CONTEXT's "60 days from impound_date + 30 days after Letter 2" (KEY OHIO COMPLIANCE RULES, line 1). The real conflict is **two**-way: Tim's verbal rule (60 days after Letter 1 *delivered/returned* — transcript, not verifiable in repo) vs. the doc+code rule. | Rewrite §3 as verbal-rule vs. implemented-rule (two definitions), and fold in V-2. WP-2 survives — see Answers. |
| V-2 | 2 | AMEND | §3 lists the clock conflict as PPI-flavored only | §3 **misses a real divergence**: POLICE `title_eligible_date = l1.sent_date + 30` with **no 60-day-from-impound floor** (models.py:813–816) — a POLICE impound whose letter went out day 10 shows title-eligible at day 40. MASTER_CONTEXT's blanket 60-day rule and Tim's 60-day mental model say otherwise. | Add "does the 60-day floor apply to POLICE?" as an explicit question in WP-2a's COMPLIANCE-TRUTH doc. |
| V-3 | 2 | NOTE (confirms spec) | §3: Tim says POLICE takes *one* letter; `letter_triggers.py` builds a 1→3→4 owner chain plus lienholder notices | **CONFIRMED.** `on_bmv_complete` creates letter_number 3 (+5 if lienholder) for POLICE; `on_letter_sent` on POLICE letter 3 creates 4 and 6 (letter_triggers.py, both functions, quoted: `_ensure_letter(vehicle, 4, 'second_notice', 'owner', due)`). Meanwhile title eligibility reads **only letter 1** for POLICE (models.py:813–816) — the code holds both beliefs at once. | None — leg verified as written. WP-2 is the right resolution point. |
| V-4 | 1/4 | **BLOCKER** | Launch Card #1: "strike through… any compliance-rule text that conflicts with the three-definitions problem described in spec section 3" | Executed faithfully, this strikes MASTER_CONTEXT's "60 days from `impound_date` + 30 days after Letter 2" — text that **matches the live code** (models.py:805–812) and sits in the same section as the settled sent-anchored rule. WP-0 would deface the one accurate compliance doc based on §3's misread. | WP-0 may STALE-mark only text that conflicts with **code at a cited file:line** — after §3 is corrected per V-1/V-2. |
| V-5 | 1 | AMEND | §2: "MASTER_CONTEXT.md — stale entries sit beside current ones **with no markers**" | Partially stale claim: MASTER_CONTEXT's header already carries a "⚠ CURRENT COMPLIANCE RULE (corrected 2026-07-29)" banner explicitly flagging old delivery-anchored entries as history ("that's history; the sent-anchored rule… is what's live"). | Soften to "inconsistently marked"; WP-0 extends an existing convention rather than importing a new one. |
| V-6 | 1 | AMEND | §2: "What does NOT exist: … a single implementation of the title-eligibility clock" | Title eligibility **is** single-sited: `PPI_TITLE_FROM_*` constants appear in models.py only (code search, 1 file), and all 12 files touching `title_eligible*` consume the models.py property. What's genuinely multi-sited is the **Letter-2 30-day rule** — four independent constants, currently consistent: task_engine.py `TASK3_DELAY_DAYS=30`, models.py:439 `letter_send_block_reason` (imports TASK3_DELAY_DAYS), letter_triggers.py `PPI_LETTER2_DAYS`, blueprints/audit.py:34 `LETTER2_SENT_GAP_DAYS=30`. | Reword; make WP-2b's "one computation site" target the four 30-day constants specifically. |
| V-7 | 3 | AMEND | WP-1(c): "importer reads impound type from the Towbook CSV column (default PPI only when absent — name the column in CP1)" | **No such column exists.** The repo's own Towbook export (Report-impounds (5).csv, header row 3) has no impound-type column; nearest signals are `Account` (apartment complex for PPI vs. police department), `Call Reason`, `Police Release Required`. D13 itself is REAL — `towbook_import.py` hardcodes `impound_type='PPI'` on the insert branch (quoted: `impound_type='PPI',` in the `Vehicle(` constructor). The fuzzy department matcher `_match_police_department` already exists in the same file. | Respec WP-1(c) as an inference rule (Account fuzzy-match → POLICE), not a column read. |
| V-8 | 4 | AMEND | WP-1(c) as written, vs. §7 guardrails | Safety hole the guardrails don't cover: the importer's **update path** applies every non-None field to existing vehicles (quoted: `for k, v in fields.items(): if v is not None: setattr(existing, k, v)`). If impound type joins `fields`, every daily CSV import silently reverts manual PPI→POLICE corrections (the correction flow shipped in PR #9), which re-routes fees and the letter chain. | Add one binding line to WP-1: impound type is set on INSERT only — never on update. |
| V-9 | 3 | AMEND | WP-2b "Done means: … tests pass"; also WP-2b scope "tests covering PPI and POLICE paths" | **No test infrastructure exists in the repo**: root listing @ a060865 has no tests/ dir and no test files; MASTER_CONTEXT calls the July integration tests "scratchpad test_*.py" (uncommitted). WP-2b as sized must also create the test harness, which strains the one-session claim. | Either add "create minimal pytest harness" to WP-2b explicitly (accept it may spill), or downgrade to scripted assertion output pasted at CP1. |
| V-10 | 4 | NOTE | Launch Card #1: push is "docs-only, harmless restart" — no reset_users step | Contradicts §7: "Until WP-5 lands, every prod deploy is followed by `python3 reset_users.py`." Practically harmless (reset_users.py only re-asserts the 9 canonical passwords; users persist in Postgres across restarts), but the card and the guardrail disagree, and Tim follows cards verbatim. | Add the shell step to the card, or exempt docs-only pushes in §7 — pick one. |
| V-11 | 4 | NOTE | Validation brief cites "the 2GB Render instance"; spec §7 is silent on instance size | MASTER_CONTEXT OPS (July 28): Render **Standard 2GB**, OOM resolved. But render.yaml's comment still says "the live service runs on the Starter/1GB instance type" — a stale comment (dashboard overrides the file). | WP-4's CP0 cost estimate must read the Render dashboard, not render.yaml. |
| V-12 | 1 | NOTE | §2: "Labels tab (Letters page) — **90 days** of labels + status + Void — Shipped" | Tab confirmed: templates/heather/letters.html (quoted: `{# ── LABELS TAB — printed-label lifecycle + void ── #}`), `ups_poll.void_orphaned_labels()`, PR #11 ("UPS label void", merged 2026-07-30 06:24Z) + PR #12 (Labels tab docs). The **90-day window specifically: NOT FOUND** — not located in searched code. | WP-0 verifies the window when it writes ALREADY-BUILT.md; WP-9 unaffected. |
| V-13 | 6 | AMEND | §8/§8b vs. MASTER_CONTEXT's open queue | Open items neither covered nor parked in §8: **relo-trans categorization** (2 transport cars sitting in inventory generating letter/storage tasks — live compliance noise), per-class tow rates (awaiting Tim's numbers), Black Book URL swap, VinAudit/Build 14 (blocked on API key), image backup + monthly purge (blocked on IT), disposition follow-ups (all: MASTER_CONTEXT "Open / not started" queue). | Add one §8 line each — parked-with-reason. Relo-trans additionally belongs in WP-0's ledger as a countable data fact. |
| V-14 | 6 | NOTE | §8b: daily-import-auto-creates-Letter-1 "decided at WP-2, not silently" | The decision is only in §8b — WP-2's §5 scope text (4 numbered rules) doesn't include it, and MASTER_CONTEXT's matching open item ("VERIFY: fresh Towbook-synced car → 1st-letter queue"; `towbook_import.py` creates no letter rows — confirmed, no CertifiedLetter reference in the file) is the exact failure that produced the July 56-letter outage gap. | Add it as WP-2a question 5 so it can't be dropped. |
| V-15 | 5 | NOTE | §9 sequence | **No dependency errors found.** WP-3 (parses `bmv_search_notes` → owner columns) is independent of WP-2's clock rules; WP-6/7/8 correctly sit behind WP-4 (staging-first per §7); no WP before WP-2b adds a new reader of either compliance rule. No reorder is forced by any citable dependency. | None. |

**Check-1 claims verified clean (no finding):** sequential gate d5f8e3a exists (commit dated 2026-07-27T02:56Z = 07/26 evening ET), enforces via `Vehicle.letter_send_block_reason` (models.py:414–443) with **no role branch** = no role bypass; both send paths named in the commit message (`letters_mark_sent` POST + `letters_create_ups_label`). Sent-anchoring live everywhere it matters: task_engine.py (Task 3 = `l1.sent_date + TASK3_DELAY_DAYS`), models.py:436–441, blueprints/audit.py:34 (comment: "Letter 2 due 30d after Letter 1 was SENT (not delivery)"), MASTER_CONTEXT banner. PRs #14/#16/#17 (auto-clear), #18 (Today view + BMV quick-link), #19 (structured owner fields, merged 2026-07-30T20:03:29Z — matches spec's "20:03"), #20, #21 all merged 07/30. `/version` NOT FOUND in code — WP-5's premise correct. `/heather/today`, `/admin/ups-test`, `/reports/date-changes`, `/admin/reclassify` + Detect-from-VIN: all confirmed in MASTER_CONTEXT dated entries + PR titles. Towbook API code dark, two env vars (`TOWBOOK_API_TOKEN`/`TOWBOOK_COMPANY_ID`). §8b's figures (56 created / 0 filled, 61 clocks started, ~551 false-flags, 4 unmatched old cars) all match MASTER_CONTEXT's July 28–29 entries. D6's "gate shipped 07/26" and D7's auto-clear-shipped read: both correct. WP-8's `build_top_nav` foundation exists (PR #1). One practical note on WP-0: the D6 row-count and D11 queue-depth need production DB access, which a local-clone Claude Code session doesn't have — expect NEEDS-LIVE-TEST/NOT FOUND there, which the card already permits.

---

## 3. THE THREE CLAIMS I ATTACKED

**1. §3's "three conflicting definitions" (the claim WP-2 is built on).** Attacked by reading all three legs at source instead of trusting the summary. **Partially falsified:** MASTER_CONTEXT and models.py agree for PPI (`max(impound+60, l2.sent+30)` — models.py:805–812 vs. KEY OHIO COMPLIANCE RULES line 1). The genuine conflict is Tim's verbal rule vs. the implemented rule — two definitions, not three — plus a divergence the spec missed entirely (POLICE has no 60-day floor, V-2). → V-1, V-2, V-4.

**2. "Labels tab shipped; Tim doesn't know it exists" (the foundation of WP-9 and the ALREADY-BUILT.md win).** Attacked because if this were wrong, the plan's fastest visible win evaporates. **Survived:** the tab is in templates/heather/letters.html with orphaned/stale badges and a void flow, backed by `ups_poll.void_orphaned_labels()` and PRs #11/#12 (merged 07/30 early AM — hours before the call, consistent with Tim not having seen it). Only the "90 days" detail is unverified (V-12).

**3. "BMV→Letter 1 server-side gate, both send paths, no role bypass (d5f8e3a)" (WP-6 re-verifies and builds on it).** Attacked by pulling the commit and the live gate code, specifically hunting for a role bypass. **Survived:** `letter_send_block_reason` (models.py:414–443) checks only vehicle/letter state — no role escape hatch; commit message enumerates both send paths. One wrinkle: d5f8e3a's own message describes the Letter-2 window as delivery-anchored — that was true on 07/26 and was corrected 07/29 (f3cca7d per MASTER_CONTEXT); today's code is sent-anchored, so the spec's "sent-anchored since 07/29" is exactly right.

---

## 4. ANSWERS

**(a) Is the §3 compliance conflict real?** Yes — but not as written. The POLICE-chain contradiction is real and confirmed at the cited code (V-3). The title-clock conflict is real but **two-sided, not three-sided**: Tim's verbal delivery-anchored rule vs. the doc-and-code rule, which agree with each other for PPI (V-1) — and the spec misses the actual third problem, POLICE's missing 60-day floor (V-2). WP-2 remains fully justified; its COMPLIANCE-TRUTH table just needs to lay out the *correct* set of disagreements.

**(b) Is WP-0 safe to launch as written?** **No — one amendment first.** It is safe for code and data (read-only, one docs commit, D6/D11 counts degrade gracefully to NOT FOUND). It is not safe for MASTER_CONTEXT: the STALE-marking instruction executes §3's misread and would strike compliance text that matches the live code (V-4). Fix §3 (V-1/V-2), rescope the STALE pass to code-cited conflicts only, reconcile the reset_users line (V-10) — then launch.

**END OF REPORT**
