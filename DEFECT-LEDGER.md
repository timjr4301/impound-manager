# DEFECT LEDGER — WP-0

Retested against `main` @ commit `5b6dea4` (2026-07-31). One row per defect from spec §4 (`BUILD — Impound Manager Remediation — Spec — 2026-07-31.md`). Every row cites file:line, PR/commit, or says NOT FOUND / NEEDS-LIVE-TEST — no uncited claims.

| ID | Observed 07/30 | Status today | Evidence | Fix WP |
|---|---|---|---|---|
| D1 | Generate Letter didn't populate owner fields | FIXED-ALREADY for new BMV-Done entries; REPRODUCED for pre-07/30 POLICE records | Both BMV Done modals now write structured `owner_name`/`address`/`city`/`state`/`zip` (commit 3f7826f, PR #19), read by `templates/print/letter.html:78` (`owner_block()` macro). Pre-07/30 POLICE vehicles still have owner data trapped in `bmv_search_notes` free text — `MASTER_CONTEXT.md:100` ("OPEN: POLICE vehicles BMV-done before 07/30..."). | WP-3 (backfill) |
| D2 | No UPS label on the record; couldn't find how to generate one | NOT REPRODUCED as "missing" — real gap is discoverability | UPS-label link exists: `templates/print/letter.html:96-97` (`📦 UPS Label` → `url_for('heather.ups_label')`), reachable only from the printed-letter page, not the vehicle detail page directly. | WP-1/WP-6 |
| D3 | Letter print has junk pages; wants one-page PDF | REPRODUCED | `templates/print/letter.html:43` — `.page-2 { page-break-before: always; ... }` — every letter (Notice of Lien lines 218-227, First/Second Notice lines 311+) unconditionally renders a second "envelope window" page. Every printed letter is at least 2 pages today, regardless of content length. | WP-1(a) |
| D4 | Generate Letter button found "on accident" | BY-DESIGN (exists, low visual prominence) | `templates/vehicles/detail.html:69-73` — "📄 Generate Letters" is one of 3 conditionally-rendered buttons (Flag / Letters / Edit) crowded into the detail-page header row, no dedicated placement. | WP-6 |
| D5 | Send button unfindable | BY-DESIGN (exists, buried) | `templates/print/letter.html:98` — "✅ Mark as Sent" only appears once already on the printed-letter view (Generate Letters → pick letter → print page). Not present on the vehicle detail page itself. | WP-6 |
| D6 | Task 2 (Letter 1) completed before Task 1 (BMV Search) in old data | NEEDS-LIVE-TEST (count) · gate confirmed shipped | Sequential gate `d5f8e3a` (2026-07-26) verified live: `models.py:414-443` `letter_send_block_reason` blocks Letter 1 send until `bmv_search_complete`. The gate is prospective only — cannot retroactively fix rows sent before 07/26. **Safe read-only check for Tim:** none available from this local clone (no prod DB access). A Render Shell read-only query would be: `SELECT COUNT(*) FROM vehicles v JOIN certified_letters l ON l.vehicle_id=v.id AND l.letter_number=1 WHERE l.sent_date IS NOT NULL AND NOT (v.heather_complete OR v.bmv_stage='COMPLETE');` — not run. | — (historical residue; gate already fixed forward) |
| D7 | Stale Possible-Release flag (2000 Honda Accord) | FIXED-ALREADY (mechanism) · NEEDS-LIVE-TEST (this specific vehicle) | Auto-clear shipped PRs #14/#16/#17 — `tina_sync.auto_clear_possible_release`, confirmed at `MASTER_CONTEXT.md:94`. **Safe check for Tim:** search "Honda Accord" (or its stock/VIN) in the app and confirm the Possible-Release flag is now clear; if still flagged, re-upload a current Towbook CSV to trigger the auto-clear path. | — (verify only) |
| D8 | Towbook export failed that day | NOT-A-BUG — upstream vendor outage | Out of code scope; long-term fix is the Towbook API path, spec sent to Gabe (gabe.a@towbook.com), awaiting his decision (`MASTER_CONTEXT.md:108`). | parked |
| D9 | A Render deploy didn't land; nobody knew | REPRODUCED — gap confirmed, no fix yet | `grep -n "/version" app.py` → **NOT FOUND**. No deploy-verification endpoint exists today. | WP-5 |
| D10 | Hub tiles dead or mystery destinations | NOT REPRODUCED | `/hub` (`app.py:3380-3383`) renders `templates/hub.html`; every tile resolves to a real endpoint — `dashboard`, `tina.pipeline`, `field_ops.index`, `heather.letters`, `invoice_camera.index`, plus 2 external links (towcommand.com, wave.com) opened in new tabs. No orphaned/BuildError-prone `url_for` calls found. Recommend Tim click each tile once live to confirm the *labels* match what he expects — that would be a wording issue, not a broken link. | — (verify labels, not code) |
| D11 | Envelope unmatched queue badly backed up | NEEDS-LIVE-TEST (depth) · query confirmed | `blueprints/envelopes.py:48-51` — `EnvelopeScan.query.filter(vehicle_id.is_(None)).filter(discarded.isnot(True))` is the live Unmatched-tab query. **Safe read-only check for Tim:** open `/envelopes` → Unmatched tab, read the count shown there directly (no DB access needed). | WP-0 counts it; ops item |
| D12 | Vehicle class not editable | NOT-A-BUG — already exists | `templates/vehicles/edit.html:64-70` has a `vehicle_class` select (light/medium/heavy) wired to `effective_storage_rate`; JS at `edit.html:236-240` auto-suggests the daily rate by class on change. Gated by `User.can_edit_vehicles` (`models.py:62`). If Tim genuinely couldn't edit it on 07/30, most likely explanation is the login used lacked `can_edit_vehicles` — worth confirming which account. | — (verify permission, not code) |
| D13 | Impound type defaults PPI instead of reading the Towbook export | REPRODUCED — confirmed real gap | `towbook_import.py:261` hardcodes `impound_type='PPI'` on every INSERT. `Report-impounds (5).csv` header row confirmed — **no impound-type column exists** in the export. `_match_police_department` (`towbook_import.py:49`) already exists but only fires post-insert on rows already typed POLICE (`towbook_import.py:274`) — since every new insert starts as PPI (line 261), the fuzzy-match can never actually flip a *new* row to POLICE today. | WP-1(c) |

## Additional counts requested by the card

- **D6 out-of-order rows:** NEEDS-LIVE-TEST — see D6 row above (no prod DB access from this clone).
- **D11 unmatched-envelope queue depth:** NEEDS-LIVE-TEST — see D11 row above; safe browser check given.
- **Relo-trans transport cars in inventory:** found **3** rows with `Account = "Paramount (ReloTrans)"` in `Report-impounds (5).csv` — stock `28687854` (row 556), `28789705` (row 621), `28789719` (row 622) — all three show an empty Release Date field, i.e. still active as of that export. ⚠ **This export's own header is dated 06/29/2026** — a month before this WP-0 pass — and `MASTER_CONTEXT.md:70` records **2** relo-trans vehicles as of the 07/28 reconciliation. The count doesn't match; likely one more relo-trans car arrived since 07/28, but this needs a live-lot confirmation, not a stale CSV. Recommend Tim verify against the current Towbook export before WP-0's ledger count is treated as final.
