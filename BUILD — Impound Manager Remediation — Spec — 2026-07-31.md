---
type: build-spec
status: design-truth v1.1 — amended 2026-07-31 per independent validation (V-1–V-15, Tom's approval relayed by Tim) — awaiting Tim's approval to start WP-0
created: 2026-07-31
authored_by: Tom (Cowork) from the 07/30 call transcript + repo read @ a060865; amended by Tim's validation session (same repo state)
run_in: Claude Code (Tim's impound-manager clone) — the Build Manager room observes, verdicts, and tracks; it NEVER builds
design_truth: this file. If MASTER_CONTEXT.md and this file disagree about the BUILD, this file wins. If they disagree about COMPLIANCE RULES, stop and resolve via WP-2 — do not pick silently.
tracker: "Impound Manager Remediation — Tracker.html (repo root)"
validation: "VALIDATION REPORT — Impound Manager Remediation Spec — 2026-07-31.md (repo root)"
---

# BUILD — Impound Manager Remediation — Spec

## 0. What this is, in Tim's words

> "I have a puzzle that's completed, but the pieces are in the wrong order. It works, the UPS thing. It works, but I can't tell how it works."

> "It cannot be chaotic. It's too chaotic right now. Clean cut and it needs to be user friendly. Need to be able to see where stuff's at."

> "I need to know what cars we impounded and need a letter sent."

**The acceptance test (binding):** Heather's five stages — Towbook export in → BMV/LKA search → Letter 1 out → returned/delivered marked → Letter 2 out — can each be completed by a person who has never seen the app before, from the task card alone, without hunting for a button. And Tim can see, on one screen, every piece of UPS postage that is out and where it stands.

**This is a remediation and rearrangement, NOT a rewrite.** The compliance engine works. The pieces are in the wrong order. Any session that proposes rebuilding from scratch has drifted — stop it.

## 1. Why this spec exists

Tim built the app in nightly autonomous Claude Code sessions (full push-to-Render permission). Speed was the point; the cost is that no one — including Tim — holds a current map of what exists, what's broken, and what's merely unfindable. On the 07/30 call he demoed a bug fixed two hours earlier, asked for a feature that already ships (the Labels tab), and described a compliance rule the code doesn't implement. This spec is the map, and the work packages repair the gap between the app that exists and the app Tim can operate, demonstrate, and hand to staff.

## 2. What already exists (verified in the repo, 2026-07-31 @ a060865)

| Asset | State | Reuse |
|---|---|---|
| Letter pipeline (task_engine.py, ~270 lines) | Working; sent-anchored since 07/29 | Untouched core |
| BMV→Letter 1 server-side gate | Shipped 07/26 (d5f8e3a), both send paths, no role bypass | Keep; WP-6 builds on it |
| UPS labels, tracking auto-poll (3-hourly), POD pull | Working | Keep |
| Labels tab (Letters page) — label lifecycle + status + Void (90-day window unverified — WP-0 confirms, V-12) | **Shipped (PRs #11/#12); Tim doesn't know it exists** | WP-9 surfaces it as "UPS Postage" |
| /heather/today — flat Overdue/Today/Upcoming task list | Shipped 07/30 (PR #18) | Keep |
| /admin/ups-test, /reports/date-changes, /admin/reclassify, Detect-from-VIN, BMV quick-link | Shipped | WP-0 inventories all of it |
| Structured owner fields in BMV Done modals | Shipped 07/30 (PR #19) | WP-3 backfills the pre-07/30 records |
| Possible-release auto-clear on reappearance (CSV + API) | Shipped 07/30 (PRs #14/#16/#17) | Keep |
| Towbook API sync code | Built, dark — needs 2 env vars when Towbook grants access | Not this build's problem |
| MASTER_CONTEXT.md | Rich but layered — a ⚠ 07/29 banner marks the compliance flip, but older stale entries are otherwise unmarked | WP-0 extends the existing STALE convention |

**What does NOT exist:** a sandbox (all testing is against production) · a single computation site for the Letter-2 30-day rule (four independent constants, consistent today, nothing keeps them so: `task_engine.TASK3_DELAY_DAYS` · `models.letter_send_block_reason` · `letter_triggers.PPI_LETTER2_DAYS` · `audit.LETTER2_SENT_GAP_DAYS`; title eligibility itself is already single-sited in `models.title_eligible_date` — V-6) · a defect ledger · inline editing in task cards · role-scoped views · deploy verification (`/version` confirmed absent) · any way for Tim to see what shipped each night · any test infrastructure (no tests/ dir, no test files — V-9).

## 3. The finding that reshapes the build

*(This section was corrected 2026-07-31 per validation V-1/V-2/V-3 — the first draft called the title clock a three-way conflict; two of those legs in fact agree.)*

**Two conflicting definitions of the PPI title clock are in circulation — plus a third question the first draft missed.** Tim's verbal rule (60 days after Letter 1 is *delivered or returned*) conflicts with the implemented rule, on which MASTER_CONTEXT.md and `models.py` **agree**: `title_eligible_date` = the later of `impound_date + 60` and `Letter 2 sent + 30` (models.py:805–812; KEY OHIO COMPLIANCE RULES). The missed third question (V-2): POLICE title eligibility is computed as `Letter 1 sent + 30` with **no 60-day-from-impound floor** (models.py:813–816) — a POLICE impound whose letter went out day 10 reads title-eligible on day 40.

A related conflict stands exactly as originally written (confirmed, V-3): Tim says POLICE impounds take *one* letter; `letter_triggers.py` builds POLICE a 1→3→4 owner chain plus lienholder notices — while title eligibility reads only letter 1, so the code holds both beliefs at once.

These cannot all be right, and every downstream feature renders whichever answer its author happened to believe. **WP-2 resolves all of it on paper, gets Tim's typed confirmation, then enforces each rule in exactly one function.** Until WP-2 passes, no WP may add a new reader of either rule.

## 4. The defect ledger seed (from the 07/30 walkthrough)

WP-0 verifies each against today's main. IDs are stable — cite them everywhere.

| ID | Observed 07/30 | Initial read (verify, don't trust) |
|---|---|---|
| D1 | Generate Letter didn't populate owner fields | Likely FIXED-ALREADY (PR #19, merged 20:03 that night). Pre-07/30 POLICE records still broken → WP-3 |
| D2 | No UPS label on the record; couldn't find how to generate one | Needs repro + discoverability fix → WP-1/WP-6 |
| D3 | Letter print has junk pages; wants one-page PDF | Template/print-CSS → WP-1 |
| D4 | Generate Letter button found "on accident" | Discoverability → WP-6 |
| D5 | Send button unfindable | Discoverability → WP-6 |
| D6 | Task 2 completed before Task 1 in old data | Historical residue; gate shipped 07/26. One-time data note → WP-0 records count |
| D7 | Stale Possible-Release flag (2000 Honda Accord) | Possibly the 07/29 false-flag wave; auto-clear shipped 07/30 → verify |
| D8 | Towbook export failed that day | Upstream vendor; out of code scope. Towbook API is the fix → parked |
| D9 | A Render deploy didn't land; nobody knew | Deploy verification → WP-5 |
| D10 | Hub tiles dead or mystery destinations | Cleanup → WP-1 |
| D11 | Envelope unmatched queue badly backed up | Operational + matching quality → WP-0 counts it; ops item |
| D12 | Vehicle class not editable | Contradicts code (effective-rate override exists) — UI/permission defect → WP-1 |
| D13 | Impound type defaults PPI instead of reading the Towbook export | CONFIRMED real gap (V-7): `impound_type='PPI'` hardcoded on insert; no impound-type column exists in the export — fix is inference, insert-only → WP-1 |

## 5. Work packages

Strictly sequential. One WP per Claude Code session unless marked. CP0 before any disk write; CP1 evidence as pasted text. A failed WP parks and reports — it never silently continues.

### WP-0 — Verify & inventory *(first; read-only; one session)*
Retest D1–D13 against main. Produce `DEFECT-LEDGER.md` (one row each: status FIXED-ALREADY / REPRODUCED / NEEDS-LIVE-TEST / NOT-A-BUG / BY-DESIGN, evidence file:line or PR#, fix WP) and `ALREADY-BUILT.md` (plain-language map of Tim's asks → existing features, with nav path + one-line "try it" for each). Also: count D6 out-of-order rows and D11 unmatched-queue depth via safe read-only checks (expect NEEDS-LIVE-TEST — a local clone has no production DB access); count the relo-trans transport cars sitting in inventory (V-13); mark stale MASTER_CONTEXT compliance text with the STALE convention (strikethrough + date + pointer to WP-2) — **ONLY text that conflicts with code at a cited file:line** (per validation V-4: the "60 days from `impound_date` + 30 days after Letter 2" line MATCHES models.py:805–812 and must NOT be struck) — the ONLY MASTER_CONTEXT edit permitted before WP-2.
*Why:* the 07/30 call showed the map is wrong in both directions — believed-broken things that work, believed-missing things that ship.
**Done means:** both files in repo root · every row cites evidence or says NOT FOUND · zero code files changed · one commit.

### WP-1 — Small visible fixes *(one session)*
(a) One-page letter PDF — print output carries no UI chrome, fits one page for standard letters. (b) D12: vehicle class editable on the ticket; letter price follows `effective_storage_rate`. (c) D13: importer **infers** impound type — the Towbook export has NO impound-type column (verified against `Report-impounds (5).csv` headers, V-7). Infer POLICE when the Account field fuzzy-matches a police department (the matcher `_match_police_department` already exists in towbook_import.py); default PPI otherwise; name the signal used in CP1. **Set on INSERT only — never on update** (the importer's update path would silently revert manual PPI→POLICE corrections on every daily import — V-8, binding). (d) D10: hub tiles either route somewhere real or are removed.
*Why:* these are the failures Tim watches happen; fixing them buys trust for the bigger WPs.
**Done means:** sample letter renders one page · class edit saves and the letter's daily rate follows it · a test CSV import sets impound_type on NEW rows only and CP1 pastes the import result line · zero dead hub tiles.

### WP-2 — Compliance truth: one clock, one chain *(two sessions; the human gate)*
**Session A (read-only):** produce `COMPLIANCE-TRUTH.md`: fetch current ORC 4513.601 / 4513.61 text and quote the operative clauses; lay the verbal-vs-implemented clock definitions and the two POLICE-chain beliefs side by side with file:line citations; propose the single rule for (1) Letter 1 deadline, (2) Letter 2 timing, (3) POLICE letter chain, (4) title-eligibility date, **(5) whether the 60-day-from-impound floor applies to POLICE** (code currently has none — models.py:813–816, V-2), and **(6) whether the daily Towbook import should auto-create Letter 1 rows** (the July-outage handoff gap — §8b, V-14). **CP-CLOCK: STOP. Tim reads the one page and types his confirmation into the doc** — if he is not certain, the named escalation is Ohio counsel or the towing-association seminar materials, not a guess.
**Session B (only after CP-CLOCK):** implement each confirmed rule in exactly one place (`models.py` property or one module), repoint every reader — specifically consolidating the four Letter-2 30-day constants (`task_engine`, `models`, `letter_triggers`, `blueprints/audit.py`) and any templates — boot backfill for stored rows that disagree (idempotent, logged), tests covering PPI and POLICE paths. **The repo has no test infrastructure (V-9):** create a minimal pytest harness as part of this session, or CP1 pastes scripted assertion output instead — Tom's manager room accepts either, but not prose claims.
*Why:* the rule already reversed once mid-operation and required a data repair; multiple definitions in circulation is how it happens again.
**Done means:** COMPLIANCE-TRUTH.md carries Tim's typed confirm · grep shows one computation site per rule · backfill logged N rows on boot · tests (or pasted assertions) pass · MASTER_CONTEXT compliance section replaced (not appended) with the confirmed text.

### WP-3 — POLICE owner backfill *(one session)*
One-time script parsing `bmv_search_notes` → structured owner columns for pre-07/30 POLICE vehicles. Dry-run by default, `--apply` flag, VehicleNote audit per change, never overwrites a non-empty field, unparseable rows listed for hand entry.
*Why:* D1's tail — those vehicles still print `[REGISTERED OWNER NAME]` on legal notices.
**Done means:** CP1 pastes the dry-run summary (N parsed / M skipped / K unparseable) · applied count logged · three spot-checked vehicles render real owner names in letter preview.

### WP-4 — Sandbox *(one session + a Render decision)*
Staging: `staging` branch → second Render service + separate Postgres seeded from a scrubbed prod copy (base64 blob tables truncated; users reset to demo passwords), unmissable STAGING banner, `UAT-CHECKLIST.md` adapted from the training guide's 8 tests. **CP0 flags the monthly Render cost for Tim's yes before creating anything** — read the instance size from the Render dashboard, NOT render.yaml (its "Starter/1GB" comment is stale; the live service is Standard 2GB per MASTER_CONTEXT OPS — V-11).
*Why:* every test so far has been against production; Heather will never be handed a production login "to try things."
**Done means:** staging URL up with banner · login works with demo creds · prod untouched (SHA unchanged) · checklist committed.

### WP-5 — Deploy verification *(one session)*
`/version` endpoint returning commit SHA + deploy time; post-deploy smoke check (script or GitHub Action: `/` and `/vehicles` return 200/302, `/version` matches pushed SHA); decision recorded on `reset_users.py` (fold into guarded boot vs. keep manual — Tim confirms at CP0, it's a behavior change).
*Why:* D9 — a deploy silently didn't land and cost a night of confusion.
**Done means:** `/version` live on prod and staging · one command answers "did my push deploy?" · reset_users decision written into this spec's STATUS LOG.

### WP-6 — Inline field editing in task cards *(two sessions; staging-first)*
Each task card in the pipeline exposes the fields its step needs — BMV Done owner fields, Mark Sent date/tracking, Confirm Delivered date — posting to the SAME routes the vehicle detail page uses. Server-side gates unchanged and re-verified (a locked task's fields render read-only with the block reason). Generate Letter / Create Label actions surfaced ON the card (D2/D4/D5).
*Why:* the agreed anchor design — "then literally I'm just working through the task." Assessed "not hard" on the call because the routes exist.
**Done means:** one vehicle walked end-to-end on staging from task cards alone, no visit to the detail page's scattered sections · gate still blocks a pre-BMV letter send · CP1 pastes the walk-through sequence + files touched.

### WP-7 — Declutter: accordions + dedupe *(one session; staging-first)*
Vehicle detail sections collapse (default open: active task + letters card only); envelope scanner reachable from exactly one place per role; nav redundancy pass.
*Why:* "way too complex looking for as simple as it is."
**Done means:** detail page above-the-fold shows task pipeline + active sections only · every collapsed section reachable in one click · no duplicate nav entries (list the removals in CP1).

### WP-8 — Role-lens views *(one session; staging-first)*
Heather's login shows only her flow (dashboard, Today, Letters, Envelopes, her guides); Lawrence keeps large-text + release list; admin roles see everything. Built on the existing `build_top_nav` per-role machinery — this is scoping, not new nav.
*Why:* "on her version she should only see what's relevant to her."
**Done means:** CP1 pastes the nav list per role (heather / lawrence / tim) · no BuildError on any role's pages · nothing reachable by Heather that her role couldn't already reach (this narrows, never widens, permissions).

### WP-9 — UPS Postage view *(one session)*
The Labels tab surfaced as a first-class "UPS Postage" nav item: all outstanding postage (label created → not yet delivered) grouped by status, with void flow; delivered/returned roll into the existing tabs.
*Why:* Tim's explicit ask, twice on the call — the feature exists, the *view of it* doesn't match how he thinks.
**Done means:** one click from anywhere shows every outstanding label with status · void exercised once on staging · Tim can answer "what postage is out right now?" on one screen.

## 6. The UX contract (what Tim sees, total)

One launch card at a time — never the whole plan. The paste block is the only thing he touches. CP evidence he copies back to the Build Manager room. Exactly one next action at the end of every manager reply. The tracker is one row per WP. Everything else is audit trail in files.

## 7. Guardrails (all binding)

- **`main` is production.** Every push deploys and restarts the app (seconds; drops chat websockets). Batch commits; never leave main broken at end of session. Until WP-5 lands, every prod deploy is followed by `python3 reset_users.py` in the Render Shell — **docs-only pushes are exempt** (passwords persist in Postgres across restarts; the restart itself is harmless — V-10).
- **One WP per session. CP0 (plan, then STOP) before any disk write. CP1 evidence is pasted text artifacts** — command output, table contents, SHAs — never prose claims.
- **Compliance rules change only through WP-2's CP-CLOCK.** The sent-anchored Letter 2 rule (corrected 2026-07-29) is not up for re-litigation by any session. `impound_date` is immutable — never a restart source.
- **No destructive data operations without dry-run + `--apply` + per-row audit note.** Never touch `damage_photos` (driver wizard — marked do-not-touch). No schema drops; new columns via the existing `run_migrations()` guarded pattern.
- **The Towbook importer's update path never changes `impound_type`** (V-8 — protects manual PPI→POLICE corrections).
- **Staging-first after WP-4** for anything touching letters, money, or permissions.
- **Park, don't halt.** A blocked item is recorded and skipped; the session continues. Hard stops only: auth failure, prod incident, or any action risking vehicle/letter data loss → stop, report, wait.
- **A drifting session** (rewriting instead of remediating, touching another WP's files, re-deciding settled rules) is failed at checkpoint, not argued with.
- **Out-of-repo escalations:** junk/salvage national database → Ohio attorney, HARD PARK, no code. Autodata Direct → Jim conversation, not a build. Towbook API → two env vars when granted, zero code.

## 8. Not building

Full rewrite (§0 — remediation only) · Towbook web scraping (API path is live with Gabe) · BMV API integration (Tim's privacy call stands until he reverses it) · junk/salvage database automation (attorney first) · Autodata Direct integration · Tow Nexus fold-in · interactive tooltip walkthroughs and in-app bot training (until the UI settles — WP-6/7/8 first) · Connecteam replacement · email/SMS digest layers.

**Also parked, from MASTER_CONTEXT's open queue (V-13):** relo-trans categorization (2 transport cars generating letter/storage tasks — WP-0 counts them in the ledger; the categorization design is a Tim+Tom conversation, not a session) · per-class tow rates (awaiting Tim's numbers) · Black Book portal URL swap (awaiting the URL) · VinAudit / Build 14 (blocked on API key) · image backup + monthly purge (blocked on IT choosing a destination) · disposition follow-ups (post-remediation queue).

## 8b. Failure → countermeasure map (the mistakes, and where each lesson now lives)

| Failure (real, theirs) | Where the lesson is baked in |
|---|---|
| July outage: 56 letters mailed outside the app; reconciliation matched zero because Towbook sync creates no letter rows | WP-0 records the handoff gap; daily-import-auto-creates-Letter-1 is now WP-2a question (6) — decided at CP-CLOCK, not silently |
| Letter-2 clock reversed mid-operation (07/29), needed a boot backfill to repair stored rows | WP-2: one rule, one function, typed human confirm, backfill pattern reused |
| ~551 vehicles false-flagged Possible Release from a date-windowed export (07/29) | Export recipe corrected upstream; WP-0 verifies flag hygiene (D7); auto-clear shipped (PRs #14/16/17) |
| POLICE letters printed `[REGISTERED OWNER NAME]` — data in free-text notes, not columns | PR #19 (structured fields) + WP-3 (backfill the stragglers) |
| A deploy silently didn't land (D9) | WP-5: /version + smoke check |
| Tim demoed a fixed bug as broken; asked for a shipped feature | WP-0's ledger + ALREADY-BUILT.md; STATUS LOG discipline so "what shipped" is readable every morning |
| Autonomous sessions implemented 2 of 7 required items and relaunched anyway (pattern from Tom's ecosystem) | CP1 evidence lists are enumerated; the manager counts items, not vibes |
| Pasted evidence disagreed with disk six times in prior manager rooms | Manager verifies in the repo (commit, files, diffs) before any PASS |
| The spec itself shipped with a misread compliance conflict (V-1) | Independent validation before Launch Card #1; findings folded in 2026-07-31 — the pattern to repeat before any future spec goes live |

## 9. Sequence & size

**WP-0 (one evening) → WP-1 (one) → WP-2a truth (one) → CP-CLOCK (Tim, minutes) → WP-2b (one) → WP-3 (one) → WP-4 (one + cost yes) → WP-5 (one) → WP-6 (two) → WP-7 (one) → WP-8 (one) → WP-9 (one).**
≈ twelve evenings. Visible wins land in the first two sessions by design. WP-6 through WP-9 are the "make it feel like one system" arc and run staging-first. *(Validation V-15: no dependency errors found in this order.)*

## STATUS LOG

- **2026-07-31 — Spec authored** (Tom, Cowork) from the 07/30 call + repo read @ a060865. Awaiting Tim's go on WP-0. Nothing built.
- **2026-07-31 — Independent validation run** (Tim's Cowork instance) against the same @ a060865: verdict **SOUND WITH AMENDMENTS**, findings V-1–V-15 (full report in repo root). BLOCKERs: V-1 (the "three conflicting definitions" was two-way — MASTER_CONTEXT and models.py agree for PPI — plus a missed POLICE 60-day-floor question) and V-4 (WP-0's STALE pass rescoped to code-cited conflicts only). All BLOCKER/AMEND findings folded into §2, §3, §4 (D13), §5 (WP-0/1/2/4), §7, §8, §8b this same day, on Tom's approval relayed by Tim. Nothing built. Still awaiting Tim's go on WP-0.
- **2026-07-31 — WP-0 executed** (Claude Code, read-only). Retested D1–D13 against `main` @ `5b6dea4`: D3/D9/D13 REPRODUCED (real, unfixed); D1 FIXED-ALREADY for new records/REPRODUCED for pre-07/30 POLICE; D7 FIXED-ALREADY (mechanism); D10/D12 NOT REPRODUCED; D2/D4/D5 exist but are BY-DESIGN discoverability gaps; D6/D11 NEEDS-LIVE-TEST (no prod DB from local clone, safe browser checks given); D8 NOT-A-BUG/parked. Relo-trans count from `Report-impounds (5).csv` (dated 06/29/2026): **3**, not the 2 recorded in MASTER_CONTEXT — flagged as needing a live-lot re-count, not treated as final. `DEFECT-LEDGER.md` + `ALREADY-BUILT.md` committed. MASTER_CONTEXT STALE-marking pass reviewed the header banner + KEY OHIO COMPLIANCE RULES (lines 1–4, 197–207): found zero text conflicting with a cited file:line — the section already matches `models.py:805–816`, `task_engine.py:32`, `blueprints/audit.py:36` — so **no strikes were applied** (nothing stale to mark). Zero code files touched. Awaiting CP1 verdict before Launch Card #2 (WP-1).
- **2026-07-31 — WP-1 executed** (Claude Code, same session). (a) One-page letters (D3): removed the forced `page-2` envelope-window page from `templates/print/letter.html` (both letter branches) — the recipient address now relies on the existing fixed-position `.addressee` block at the top of the letter instead of a duplicated second page; redundant same-page address repeats also removed, genuine owner/lienholder cross-reference content kept. ⚠ Exact envelope-window physical position was not available to verify against — Tim confirmed via AskUserQuestion he wants the address merged onto page 1, but a real test-fold against an actual envelope is still needed before relying on this for mail that must show correctly through the window. (c) Impound-type inference (D13): `towbook_import.py`'s insert branch now fuzzy-matches the Account field against `police_departments` (reusing the existing `_match_police_department`) — POLICE on a match, PPI otherwise; still INSERT-ONLY, `impound_type` remains absent from the update-path `fields` dict (V-8 protected). (b)/(d) D12 (class editable) and D10 (hub tiles) needed no change — WP-0 already found both working. **Verified, not just claimed:** ran the actual Flask app + `/api/import-towbook` endpoint against a throwaway SQLite DB (not prod) — PPI-account row → `impound_type='PPI'`; POLICE-account row → `'POLICE'` + correct `police_department_id`; a same-stock re-import with a different, non-matching Account after a simulated manual correction to POLICE left `impound_type` unchanged (`PASS`) — proves the anti-revert guarantee, not just reads it off the code. Also rendered all three letter kinds (PPI First Notice/owner, PPI Second Notice/lienholder, POLICE Notice of Lien) through the real `/letters/<id>/print` route: all 200, zero `page-2` divs in any of them, ~5.2–5.7k characters of HTML body each — comfortably one page. `py_compile` and Jinja `env.parse()` both clean. Awaiting CP1 verdict before Launch Card #3 (WP-2a).
- **2026-07-31 — WP-2a executed** (Claude Code, read-only, same session). Produced `COMPLIANCE-TRUTH.md`. Code side (100% confidence, direct reads): confirmed §1's table — PPI/POLICE Letter 1 deadlines, the four-constant Letter-2 30-day trigger (V-6), PPI title formula (`models.py:805-812`), POLICE title formula with no floor (`models.py:813-816`, V-2), and the full POLICE letter chain (`letter_triggers.py:50-76`, V-3). Statute side: sourced ORC 4513.601/4513.61/4505.101 via repeated web fetches, which gave **inconsistent results across identical repeat queries** including one fabricated detail (a nonexistent "30/45-day notice" requirement) that was caught and discarded — flagged explicitly rather than presented as settled. Two independent fetches of the actual title-issuance statute (ORC 4505.101(B)(3), cross-referencing 4513.601(F)) converged on: the PPI 60-day title clock runs from the **first notice's receipt or confirmed delivery-failure** — not from `impound_date`, not from Letter 2's sent date. **This is the opposite of what the code and MASTER_CONTEXT implement, and it matches Tim's verbal rule** — the reverse of what V-1 concluded (V-1 read the code+doc agreement as evidence Tim's memory was stale; this research suggests the implementation may be the stale side). POLICE's statutory basis is unresolved (**NEEDS COUNSEL**), as is whether the POLICE second-notice chain is statutorily required at all. Did not touch any code — this is Session A only. **Hard stop at CP-CLOCK: WP-2b does not start until Tim types his confirmation into `COMPLIANCE-TRUTH.md`, and given the reversal found here, counsel/seminar-material confirmation is recommended before he does, not just offered as a fallback.**
- **2026-07-31 — CP-CLOCK (partial) + WP-2b (partial) executed** (same session). Tim independently noticed a title-eligibility date "calculated on the wrong day" earlier the same day, before being shown this document's finding — corroborating evidence, not just my research. Given a binary choice (lock in now vs. wait for counsel), **Tim explicitly chose to lock in item 4 now and verify with counsel/seminar materials afterward as a backstop** — his call to make, made with the uncertainty clearly disclosed beforehand. Items 1, 2 confirmed unchanged (no conflict). **Items 3 and 5 (POLICE letter chain, POLICE title-clock question) and item 6 (auto-create Letter 1 on import) remain explicitly OPEN and untouched** — Tim's confirmation was scoped to item 4 only; nothing about POLICE was decided, so nothing about POLICE was changed. WP-2b implemented item 4: `models.py` — `PPI_TITLE_FROM_LETTER1_DELIVERY = 60` replaces the two old constants; new `CertifiedLetter.delivery_or_undeliverable_date` property (delivery scan wins, confirmed RTS falls back to sent_date+3 — reusing, verbatim, the delivery-or-RTS logic `task_engine.letter_delivery_date` used pre-2026-07-29 before Task 3 became sent-anchored, since that logic was already shipped and understood); `title_eligible_date`/`title_blocked_reason` rewritten for PPI to read Letter 1's outcome instead of impound_date/Letter 2; two stale comments in `app.py` (RTS-restart flow, Letter Clock Restart route) corrected since they asserted "title_eligible_date always reads impound_date" which is no longer true for PPI. `title_eligible_date` is a computed `@property`, not a stored column, so **no boot backfill was needed** — every page reflects the new formula immediately on deploy. `MASTER_CONTEXT.md` KEY OHIO COMPLIANCE RULES section 4 rewritten to the confirmed PPI rule, explicitly noting POLICE is unchanged/unresolved. **Verified, not just claimed:** 8 scripted assertions against a throwaway SQLite DB (not prod) — delivered-anchor math, RTS-anchor math (sent+3+60), the "sent but delivery unknown" case correctly returns `None` even though the *old* impound_date+60 would have long since passed (this is the actual behavior change, proven, not just described), the correct `title_blocked_reason` text in both blocked states, POLICE's formula confirmed byte-for-byte unchanged, and an `is_title_eligible` boundary check — all 8/8 passed. `py_compile` clean on both changed files. **Items 3, 5, 6 remain gated exactly like item 4 was — no future session may implement any of them without its own explicit CP-CLOCK-style confirmation, per COMPLIANCE-TRUTH.md's closing line.**
- **2026-07-31 — WP-2b follow-up: Returned-to-Sender outcome choice** (same session). Tim's own follow-up on item 4: the single existing "Returned to Sender" action (`app.py::letters_returned_to_sender`) always superseded the letter and restarted at a fresh Letter 1, with no way to record a genuinely valid (non-address-error) return — meaning a letter that legitimately couldn't be delivered (refused, unclaimed, moved with no forwarding) could never itself start the 60-day title clock; it would just restart indefinitely. Added a required `outcome` choice to the form (`templates/vehicles/detail.html`'s RTS modal): **address_error** (original behavior — supersede + fresh Letter 1) or **valid_return** (new — no supersede, no restart; `return_to_sender`/`returned_date` stand as the compliance record on the current letter). Also improved `CertifiedLetter.delivery_or_undeliverable_date` (`models.py`) to prefer the real staff-entered `returned_date` over the old `sent_date+3` estimate whenever it's available — an actual recorded date beats a guess. **Verified, not just claimed:** ran both outcomes through the real `/letters/<id>/returned-to-sender` route against a throwaway SQLite DB (not prod), logged in as `heather` (the route's actual required role) — `valid_return`: letter not superseded, vehicle stays Round 1, `title_eligible_date` = returned_date+60 exactly · `address_error`: original letter superseded, vehicle advances to Round 2, new Letter 1 is unsent, `title_eligible_date` correctly `None` until the new round has its own outcome — both PASS. `py_compile` and Jinja `env.parse()` clean.
- **2026-07-31 — WP-3 (code, not yet run on prod)** (same session). New `backfill_police_owner_from_notes.py` — one-time script recovering owner name/address for pre-07/30 POLICE vehicles whose BMV Search result only ever got typed into the old free-text `bmv_search_notes` box (PR #19, 2026-07-30, is what added the structured owner inputs; earlier records have nothing structured to read, so their Notice of Lien still prints `[REGISTERED OWNER NAME]` — D1's tail). Candidates: `impound_type == 'POLICE'`, `bmv_search_notes` populated, `owner_name` blank — dateless and exact, rather than relying on a date cutoff. Parser is deliberately narrow: it only accepts notes whose last line reads as `City, ST ZIP`, with 1–2 lines above it as name (+ optional street, comma-split); anything not matching that shape, or whose name-line contains a digit or reads like "no record"/"unknown"/"n/a"/etc., is left unparsed for hand entry — **never guesses** on a document with legal weight. Never overwrites an already-filled field. Dry-run by default; `--apply` commits + writes a `VehicleNote` per changed vehicle. **Verified, not just claimed:** 21 checks against a throwaway SQLite DB (not prod) — 9 parser unit cases (clean 3-line, comma-combined 2-line, name-only, lowercase-state normalization, and 5 rejection cases: "no record", a digit-containing line, 4+ ambiguous lines, no city/state/zip shape at all, empty notes) all correct, plus a full dry-run → `--apply` cycle proving it: finds the parseable candidate, correctly lists an unparseable one for hand entry, leaves an already-filled owner untouched and never even lists it as a candidate, excludes a PPI vehicle entirely, writes nothing during dry-run, and writes the exact expected fields + one audit `VehicleNote` on `--apply`. **This session had no access to real production `bmv_search_notes` text to validate the parser's assumed format against** — that's the one thing only Tim's Render Shell dry-run can confirm. Per spec's "Done means," CP1 for this WP is Tim pasting the real dry-run summary (N parsed / M skipped / K unparseable) back before any `--apply`.
- **2026-07-31 — WP-3 CP1: real production dry-run pasted.** Tim ran `python3 backfill_police_owner_from_notes.py` in the Render Shell: `No POLICE vehicles found with notes-only owner info (owner_name already blank + bmv_search_notes populated). Nothing to do.` Zero candidates on live prod — either D1's tail already got resolved some other way since 07/30, or the remaining blank-owner POLICE vehicles have no notes text to recover from at all (a fresh BMV search, not a backfill, is what those need). No `--apply` run, none needed. **WP-3 closed.** Also found and read Heather's own handwritten process notes (uploaded by Tim) — her documented Returned-to-Sender workflow ("check for reason: if restart isn't needed, just attach & move on; if it does, restart them") independently matches the address-error-vs-valid-return split built earlier in WP-2b's follow-up, confirming that design after the fact rather than before. Her notes also surfaced an unrelated, real gap: the envelope scanner doesn't show the return reason or the address a letter was sent to when she's reviewing a scanned envelope — flagged, not yet scheduled to any WP.
- **2026-07-31 — Out-of-sequence fix: envelope scan reason + address visible at the decision point.** Per Heather's notes ("WILL NEED TO BE ABLE TO SEE FULL ADDRESS SENT TO & REASON FOR RETURN IN SCAN"). The vehicle detail page's "Scanned Return Envelope" card (`templates/vehicles/detail.html`) now shows the outcome badge (Delivered/Address Issue/Service Disrupted/Unknown — same categories already used on `/envelopes`), the AI's `ai_read_summary` (existing property, previously only shown on the Unmatched tab), and the vehicle's current owner address — right next to the Returned to Sender button, so she has what she needs without leaving the page. `/envelopes`' Matched tab (`templates/envelopes/index.html`) gained the same reason + address as a new column. No new fields, no schema change — both `outcome`/`ai_read_summary`/`owner_address` already existed, they just weren't surfaced where she actually needs them. **Verified, not just claimed:** rendered both pages through the real routes against a throwaway SQLite DB (not prod) with a seeded ADDRESS_ISSUE scan — both 200, both show the badge, the AI summary, and the owner address. Jinja `env.parse()` clean on both files.
- **2026-07-31 — WP-4 code side.** CP0 cost cleared: Tim approved the cheapest viable tier (~$13-15/month: small web service + small Postgres) over matching production's Standard tier. What this session could build: `app.config['IS_STAGING']` (default False, only True when the env var is explicitly set — can't accidentally show on prod) drives an unmissable yellow banner in `base.html`; `scrub_for_staging.py` — a one-time script that nulls/empties every base64 image and document blob across 9 tables (Vehicle VIN/converter photos, CertifiedLetter returned-envelope/POD images, EnvelopeScan images, DamageReport signatures/PDFs, DamagePhoto images, the legacy VehicleNotice label cache, VehicleDocument/GeneralDocument/VehicleDamagePhoto files) while leaving every other field intact, refuses to run without `--confirm-staging`, and only ever touches its own `DATABASE_URL` — same one-database-only convention as every other script in this repo, so there's no way for it to reach across and touch production; `UAT-CHECKLIST.md` adapting the training guide's 8 existing tests plus 2 new ones covering tonight's compliance fix; a `staging` git branch created from this commit. **What this session could NOT do: create the actual Render Postgres instance or Web Service** — no Render API/CLI access here, confirmed by checking for a Render MCP tool (none found). That step needs Tim in the Render dashboard. **Verified, not just claimed:** scrub script run against a throwaway SQLite DB seeded with real-shaped blob data in all 9 tables — 16/16 checks passed, including that it refuses to run at all without the confirm flag, and that non-blob fields (stock_number, dates) survive untouched. `py_compile` clean.
- **2026-07-31 — WP-4: added `clone_prod_to_staging.py`.** Checked this machine for `pg_dump`/`psql` (the obvious way to copy a Postgres database) — neither is installed, so the original plan of handing Tim a `pg_dump | psql` command would have failed on the first try. Wrote a pure-SQLAlchemy alternative instead: reflects the entire schema off the source database, truncates every table on dest in reverse dependency order, then copies every row in forward dependency order — no hardcoded table list, so it can't drift out of sync with the schema the way a hand-maintained list would. Takes `--source`/`--dest` as required explicit arguments (nothing from `DATABASE_URL` or any ambient env var), refuses if they're equal, refuses without `--i-understand-this-truncates-dest`, never writes to source. **Verified, not just claimed:** ran it against two real throwaway SQLite databases (not prod) — confirmed it refuses on matching source/dest, refuses without the flag, truncates pre-existing stale data in dest, and correctly copies a parent row (Vehicle) and its FK-dependent child (CertifiedLetter) across all 33 reflected tables including the auto-seeded `police_departments`/`users` tables. 4/4 checks passed.
- **2026-07-31 — WP-4 COMPLETE: staging live, real data cloned, verified end to end.** Tim created the actual Render infrastructure by hand (walked through step by step): `impound-manager-staging-db` (Basic-256mb, 1GB storage, ~$6.30/mo) and `impound-manager-staging` web service (Starter, ~$7/mo) on the `staging` branch, environment copied from production (minus the unused `BASE44_*` vars Tim confirmed are dead — that integration was retired in favor of the in-house disposition pipeline). First deploy went Live; Tim confirmed the yellow STAGING banner renders and login works.

  Running the real clone against production surfaced three real issues no amount of synthetic SQLite testing could have — each fixed and re-verified before the next attempt: (1) production's database also holds ~33 tables belonging to BJ Books (a separate app that, it turns out, has no database of its own in the Render account — it's quietly sharing Impound Manager's), which `clone_prod_to_staging.py` now detects and skips automatically by only trusting tables the freshly-created staging schema actually has; (2) `police_departments` carries `hook_fee`/`storage_fee` columns left over from before the `tow_rate`/`storage_rate` rename, and `vehicles` carries 6 orphaned `ai_*` columns from before AI damage analysis moved to its own `DamageReport` table — both now filtered per-table against the destination's real columns; (3) the first two real attempts both failed outright (lost all progress) when the SSL connection dropped mid-transfer on tables carrying large embedded images/PDFs (`envelope_scans`, `vehicle_documents`) — because the whole clone ran as one giant transaction. Rewrote to chunk both reads and writes (20 rows/connection at a time) with per-chunk commits and connection-retry, so a drop only costs one chunk instead of the whole run; verified with a dedicated test proving an earlier table's data survives a later table's total failure.

  **Final real run:** ~10,700+ rows copied across 21 Impound Manager tables (3,485 vehicles, 1,228 certified letters, 4,736 vehicle notes, 542 damage photos, 242 general documents, 212 of 230 title/LKA documents, 147 envelope scans, 107 BMV scan records, 41 users, and more). 37 rows skipped, all for cited, non-mysterious reasons: 1 vehicle with a value too long for its current column size, 6 rows cascading from that one skipped vehicle (letters/notes referencing it), and 18 title/LKA-document rows that hit repeated connection drops even after 3 retries each (large PDFs; a targeted retry could recover these later, not blocking). `scrub_for_staging.py --confirm-staging` then blanked 1,100+ real photo/document rows across all 9 blob-holding tables; `reset_users.py` reset all 10 accounts to documented defaults. **WP-4's "Done means" is fully met:** staging URL up with banner (✓ Tim-confirmed) · login works with demo creds (✓) · prod untouched, SHA unchanged (✓ — every write throughout this went only to the staging database URL) · checklist committed (✓, `UAT-CHECKLIST.md`). Only remaining step is a human actually running that checklist.

- **2026-07-31 — Tim ran UAT-CHECKLIST.md live on staging, all 10 tests PASSED, several real defects caught and fixed along the way (staging only, `staging` branch, prod never pushed during this pass):**
  - Test #1 (Import) first hit a real bug, not a Towbook parsing issue: `clone_prod_to_staging.py` copied rows in with their real production `id`s, which never advances Postgres's own auto-increment sequence — the first normal insert (no explicit id) collided with an already-cloned row. Wrote `fix_staging_sequences.py` (one-time repair, run by Tim in the staging Render Shell — 33 sequences fixed) and taught `clone_prod_to_staging.py` to reset sequences automatically after any future clone.
  - Test #2 (UPS label) exposed a real risk: staging inherited production's live UPS API credentials, and `ups_api.py`'s `_BASE` is UPS's real, billed endpoint — no staging safety switch existed. A 400 error from bad test data accidentally prevented a real charge; a valid address would not have been so lucky. Tim chose "fake it": `ups_api.py` now short-circuits `create_label`/`void_shipment`/tracking lookups on `IS_STAGING`, fabricating a `1ZFAKE...`-prefixed response instead of ever calling UPS for real. Also replaced the blank fake-label image with an explicit "STAGING — not a real label" placeholder after Tim asked whether the blank print preview was real.
  - Live-caught, in passing: the PPI title-eligibility explainer text on `vehicles/detail.html` and `vehicles/new.html` still described the pre-WP-2b formula (impound_date/Letter 2 based) after the calculation itself was already corrected — fixed to read `vehicle.title_blocked_reason` (the same single source of truth the calculation uses) instead of a separate hardcoded string.
  - **Confirmed live, not just theorized: COMPLIANCE-TRUTH.md item 6 (daily import never creates Letter 1) is a real, current gap** — a vehicle imported via Daily Intake CSV during Test #1 has no path in the app to ever get its Letter 1 created (`app.py`'s `_resolve_letter` explicitly refuses: "letter_number 1 is never created here — intake owns it," and the CSV importer never creates it). Only the manual "+ New Impound" form creates Letter 1 correctly. Not touched — item 6 remains gated exactly as `COMPLIANCE-TRUTH.md` requires, pending Tim's fresh confirmation.
  - **Also found and NOT touched (flagged for WP-6/7):** for POLICE impounds, the vehicle detail page's Task Pipeline and title-eligibility both track letter_number=1 ("Notification Letter" / "Police Notice of Lien"), while the Generate Letters hub separately offers "First Notice (Owner)"/"Second Notice (Owner)" (letter_number 3/4) with near-identical naming — Tim sent 3 and 4 believing that was the compliance-critical letter, then had to backtrack. The "Returned to Sender" reason-choice modal is also only wired up for letter_number 1/2, not 3/4 — clicking it on a POLICE owner notice does nothing (no button exists there at all). Tim's own words after hitting this live: "this letter thing is not very clear it should be more concrete and simple... why are there two ways to generate letters? there should be one." This is exactly WP-6/WP-7's mandate — not implemented here, logged as direct evidence for that work.
  - 18/230 title/LKA documents missing from the staging clone (repeated connection drops on large PDFs during WP-4) — confirmed still a known, low-priority, non-blocking gap; not touched this session.
  - All fixes this session verified against a real running staging instance (fast-forwarded from `main`, pushed to `staging` branch only — `main`/production never pushed, SHA unchanged throughout).

- **2026-07-31 — WP-5 executed** (Claude Code, same session, after Tim's go). **`reset_users.py` decision (CP0, Tim confirmed): stays MANUAL, not folded into guarded boot.** `seed_default_users()` already only ever *creates* missing accounts and deliberately never resets an existing password (its own docstring: doing so would silently undo a real password someone set via `/admin/users`). Auto-running `reset_users.py` on every boot would reintroduce exactly that problem on every deploy. Tim's call, matching the recommendation. `/version` endpoint added (`app.py`) — no login required, returns `RENDER_GIT_COMMIT` (a real env var Render sets automatically, no config needed) plus the process's own boot time (a fresh Render deploy always spins up a new process, so boot time and deploy time are the same thing) and `is_staging`. `smoke_check.py` added — `python3 smoke_check.py <url> [expected_sha]` hits `/`, `/vehicles`, and `/version`, PASS/FAILs each, exits 0/1. **Verified, not just claimed:** ran the real Flask app against a throwaway SQLite DB (not prod) with an actual local server bound to a port (not just the test client) — `/version` returned real JSON with a live boot timestamp; `smoke_check.py` run against that real server: all 3 checks PASS with no expected SHA, correctly FAILs with "cannot compare" when an expected SHA is given but `RENDER_GIT_COMMIT` isn't set (matches real local-dev conditions; will resolve automatically on Render where that env var is always present). `py_compile` clean on both changed/new files.

- **2026-07-31 — COMPLIANCE-TRUTH.md item 3 CONFIRMED and IMPLEMENTED, mid-WP-6.** Tim, unprompted, confirmed his original verbal rule stands: POLICE impounds get ONE letter (Notice of Lien, letter_number=1), not the coded 1→3→4(+lienholder) chain — same "lock in now" pattern as item 4, statutory basis still unconfirmed by counsel. `letter_triggers.py`: `on_bmv_complete()` is now a no-op (kept only because `blueprints/heather.py`'s route still calls it), `on_letter_sent()`'s POLICE branch removed outright. `app.py`: `LETTER_SLUGS['POLICE']` now only has the `police` slug; Generate Letters hub no longer offers First/Second Notice (Owner/Lienholder) for POLICE. PPI's letter 1→2 chain and lienholder 5/6 completely untouched. Existing already-sent letter_number 3/4/5/6 rows on real vehicles are NOT deleted or altered — real letters that were really mailed stay real history, this only stops generating new ones. `COMPLIANCE-TRUTH.md` updated to match (item 3 moved from OPEN to CONFIRMED, header/closing-gate lines updated). **Verified, not just claimed:** 9/9 scripted checks against a throwaway SQLite DB (not prod) — POLICE vehicle gets nothing beyond letter 1 through BMV-complete and letter-1-sent; PPI vehicle's full chain (2, 5, 6) still fires exactly as before; the real Generate Letters hub route, hit through the actual Flask test client, offers "Police Notice of Lien" and nothing else for a POLICE vehicle. `py_compile` clean.

- **2026-07-31 — WP-6 Session 1 of 2 executed** (Claude Code, staging-first, after Tim's "go"). Task 2 and Task 3 on the vehicle detail page's Task Pipeline now have the same inline pop-up pattern Task 1 (BMV Search) already used, instead of linking to a separate `/letters/<id>/mark-sent` page: a "Send Now" button opens a modal with "Create UPS Label & Mark Sent" plus a manual Date Sent/Tracking Number fallback, posting to the exact same routes (`letters_mark_sent`, `letters_create_ups_label`) the old standalone page used — the server-side compliance gate (`Vehicle.letter_send_block_reason`) is untouched code, just re-verified. Submitting now redirects back to the vehicle detail page (`from_vehicle_card=1` hidden field on both forms) instead of the dashboard or a separate screen — a new `_pending_inline_label_cache` (module-level, same one-worker-process pattern `_pending_label_2_cache` already used) carries the just-created label(s) across that redirect so the modal can show the result, auto-reopened via a small script when the vehicle page loads with a pending label. The old standalone `letters/mark_sent.html` page and its routes are completely unchanged for anyone still reaching them another way (e.g. from the Generate Letters hub) — WP-6 only adds the inline path, WP-7 handles removing the now-redundant old one. Scope deliberately limited to the primary Letter 1/Letter 2 pipeline (now item 3's ONLY letters for POLICE too, so no scope gap). **Verified, not just claimed, against the real Flask app + a throwaway SQLite DB (not prod), via the actual test client hitting real routes:** gate still blocks Mark Sent before BMV is done · manual Mark Sent from the inline modal correctly redirects to the vehicle page (not `/dashboard`) and Letter 2 auto-creates · Create UPS Label from the inline modal (staging fake-UPS path, `IS_STAGING=true`) redirects to the vehicle page with the fake `1ZFAKE...` tracking number and the "STAGING — not a real label" placeholder actually rendering inside the real modal HTML (15/15 checks) — caught and fixed a real bug here first: the modal only rendered while a letter was still unsent, so the auto-open script referenced a modal that didn't exist once the letter became sent; fixed by also rendering the modal, button hidden, whenever a pending inline label exists for an already-sent letter. `py_compile` and Jinja `env.parse()` both clean. Session 2 (Task 4 + Generate Letter buttons on the cards) not started.

- **2026-07-31 — WP-6 Session 2 of 2 executed** (Claude Code, staging-first, same session). Task 4 ("Ready to File") gets the same inline modal treatment as Tasks 1–3 — BMV Receipt # + Notes, posting to the existing `file_title` route, redirecting back to the vehicle page. "Print Letter" links added directly on the Task 2/Task 3 cards (D4/D5 — "Generate Letter button found on accident" — now it's right there, no hunting). **Found and fixed a real pre-existing bug while verifying, not introduced this session:** Task 4's card computed its own "eligible" date from `l2.sent_date + 45 days` — a stale proxy nobody updated when WP-2b (item 4) changed PPI title eligibility to anchor off Letter 1's delivery/RTS date instead. The real gate (`file_title` route's server-side `vehicle.is_title_eligible` check) was always correct, so nothing could ever be filed early — but the card itself could show or hide "File Now" at the wrong time relative to the actual rule. Now reads `vehicle.is_title_eligible`/`title_eligible_date` directly, same single source of truth as everywhere else; the "not yet" state also now shows `vehicle.title_blocked_reason` (e.g. "Waiting on Letter 1 delivery confirmation or return-to-sender") instead of a generic, sometimes-wrong "Locked — complete Task 3 first." **Verified, not just claimed:** scripted checks against a throwaway SQLite DB (not prod) via the real Flask app + test client — a title-eligible vehicle shows "File Now," submitting the inline modal creates the `TitleFiling` record, saves the BMV receipt number, advances the vehicle to `TITLE_FILED`, and redirects back to the vehicle page (9/9 checks); a separate not-yet-eligible vehicle (Letter 1 sent but not delivered) correctly shows no "File Now" button and the accurate blocked-reason text instead of the old generic one. `py_compile` and Jinja `env.parse()` both clean. **WP-6 is now fully done** (both sessions) — pushed to `staging` only, `main`/production not touched.

- **2026-07-31 — WP-7 executed** (Claude Code, staging-first, same session, one background agent used for the bulk mechanical section-wrapping). Tim's ask at CP0: keep Year/Make/Model (already the page's H2 title, no change needed), last 6 of VIN, and Invoice # visible above the fold too — added to the header subtitle line alongside impound type/date/days-in-storage. Every other vehicle-detail section now collapses by default (click header to expand — same pattern the page already used for "Towbook Data"): Vehicle Information, Owner/Addressee, Lienholder, Financial, Additional Charges, Actions, Title Filing, Scanned Return Envelope, Title Packet, Driver Damage Reports, Damage Photos, Notes, Documents (LKA/Title Search), General Documents, VIN Photo Verification. Only the Task Pipeline and Certified Letter Timeline ("letters") stay open by default, per spec. Headers with an embedded action button/badge (Edit pencil, Generate BMV 4202, Assess Damage, etc.) got the button left untouched and the collapse-toggle wrapped around just the title text, so the button stays independently clickable without also toggling the section. Zero logic/route/field changes anywhere — purely a visual collapse wrapper. Nav redundancy pass: checked `build_top_nav` (app.py) and Heather's dashboard for duplicate destinations — found none; the two envelope-related dashboard buttons (`heather.envelope_scan` single-camera-scan vs `heather.bulk_envelope_scan` file-upload) are genuinely different features, not duplicates of the same page, and `envelopes.index` (the review/matched-unmatched list) has exactly one nav entry. No removals needed or made. **Verified, not just claimed:** Jinja `env.parse()` clean throughout (checked repeatedly during the section-by-section work, not just at the end); real Flask test-client renders (throwaway SQLite DB, not prod/staging) confirmed 200s across multiple vehicle states (fresh vehicle, one with a sent letter + tracking number, one with a general document, one with a damage photo) with the header additions, all new collapse ids, and real content all present; confirmed Task Pipeline and Certified Letter Timeline have no collapse wrapper (always open) while their content (e.g. a tracking number) is visible without expanding anything.
